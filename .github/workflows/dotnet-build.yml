name: Meu Fluxo de Trabalho

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v2

      # Adicione outras ações pré-existentes aqui, se necessário.

      - name: Executar script personalizado
        run: |
          # Seu script personalizado aqui

